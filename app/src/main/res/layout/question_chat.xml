<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/questionChat_view"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/questionChat_image"
        android:layout_width="@dimen/icon_size"
        android:layout_height="60dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@mipmap/ic_launcher_round"/>

    <TextView
        android:id="@+id/questionChatTitle_text"
        android:layout_width="0dp"
        android:layout_height="20dp"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="16dp"
        android:textStyle="bold"
        app:layout_constraintEnd_toStartOf="@+id/questionNumberUsers_text"
        app:layout_constraintStart_toEndOf="@+id/questionChat_image"
        app:layout_constraintTop_toTopOf="@+id/questionChat_image"
        tools:text="Title" />

    <TextView
        android:id="@+id/questionNumberUsers_text"
        android:layout_width="58dp"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        android:text="TextView"
        app:layout_constraintBottom_toBottomOf="@+id/questionChatTitle_text"
        app:layout_constraintEnd_toEndOf="parent" />

    <TextView
        android:id="@+id/questionChatQuestion_text"
        android:layout_width="0dp"
        android:layout_height="20dp"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        app:layout_constraintEnd_toEndOf="@+id/questionNumberUsers_text"
        app:layout_constraintStart_toEndOf="@+id/questionChat_image"
        app:layout_constraintTop_toBottomOf="@+id/questionChatTitle_text"
        tools:text="Question" />
</androidx.constraintlayout.widget.ConstraintLayout>